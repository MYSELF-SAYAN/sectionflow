/**
 * Core file sources for the v2 architecture.
 *
 * These are produced by scripts/sync-sources.mjs from the canonical source in
 * src/library/core/ — never hand-edited. Re-run `npm run sync:sources` to
 * refresh them when the canonical core changes.
 *
 *   TYPES_SOURCE       → written to <user>/components/sectionflow/core/types.ts
 *   SECTION_FLOW_SOURCE → written to <user>/components/sectionflow/core/section-flow.tsx
 *   REGISTRY_SKELETON  → written to <user>/components/sectionflow/core/registry.ts
 *                        (empty map; `add` appends entries incrementally)
 *   TIMING_OVERRIDES   → read by `add` to decide whether to attach a `.timing`
 *                        override per installed transition
 */
export { TYPES_SOURCE, SECTION_FLOW_SOURCE, REGISTRY_SKELETON, TIMING_OVERRIDES, } from './generated/core-sources.js';
//# sourceMappingURL=core-files.d.ts.map